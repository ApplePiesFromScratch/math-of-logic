# “Contradiction” is several G

`python3 contradiction.py`

One word. These do not share a remainder.

---

## The mechanics

| id | what happens | remainder | school name it gets |
|---|---|---|---|
| C1 | two combinators, same meets, two letters | `(1,2)` vs `(1,0)` | inconsistency of definitions |
| C2 | two compose-orders | `(1,2)` vs `(1,1)` | commutator, curvature, paradox of path |
| C3 | write leaves V | `(1,2) ∉ {0,1}²` | invalid, undefined, explosion into a bigger host |
| C4 | val-eq of two mints | same pair, two generators | same thing / contradiction of origins |
| C5 | wrap vs host on ink `2` | `2==0` False and True | char-p contradiction |
| C6 | wanting one row to be 1 and 0 on V2 | no such row | P and not-P |
| C7 | same letter, two ANDs on V3 | `¼` vs `½` | many-valued “inconsistency” |
| C8 | from 0 write every letter of V | whole V | explosion / ex falso |
| C9 | dead / dead | θ | 0/0, undefined, contradiction in the data |
| C10 | combinator not a map of this V | TypeError | type clash, sort error |
| C11 | two reads not locked | `0≠1` | theory conflict (Einstein vs vacuum row) |
| C12 | empty V closes | 0 leaks | vacuous consistency |
| C13 | keep host `2` and wrap `0` as one letter | two books | 0=1 in the quotient |
| C14 | two pairs, one Born | 1=1 on the project | same state / contradiction of phase |
| C15 | two filters, two mins | vac vs wall | two vacuums |

C6 never printed a row that is both letters. V2 has no “P and not-P” as a value. People still say contradiction when they *ask* for that row.

C8 is a G you can write. Most machines do not. Calling every leftover “explosion” hides that.

C12 is consistency of a corpse. Close ≠ “no contradiction.”

---

## What the word is mixing

- **Disagreeing G** (C1, C2, C7, C11, C15) :  leftover. Keep both numbers.
- **Left V** (C3) :  leak. θ or μ or other combinator.
- **Host clash** (C5, C10, C13) :  two books on one glyph.
- **Identified mints** (C4, C14) :  project or val-eq.
- **Asked-for impossible row** (C6) :  not an output, a request.
- **Annihilator write-all** (C8) :  a specific G.
- **Hole** (C9) :  θ.
- **Empty success** (C12) :  vacuous.

Those are not one incompatibility.

---

## Logic textbook bundle

“Contradiction” there usually means C6-request plus C8-G plus a metalanguage `=`.  
`P∧¬P ⊢ Q` is C8 if the calculus has explosion.  
On V3, `½∧½` is min `½` or prod `¼` (C7). No explosion.

Paradoxes:
- liar / Russell :  metalanguage glyph on the object line (taxonomy #19) plus hidden-total `∈` (#20)
- 2=0 and 2≠0 :  C5/C13
- measurement “contradiction” :  C2 plus C14
- GR vs QM “contradiction” :  C11 until a lock-G exists

---

## Doctor

Do not book leftover as explosion.  
Do not book θ as False.  
Do not book vacuous close as consistency.  
Name C1-C15 (or the next one a runner prints).
