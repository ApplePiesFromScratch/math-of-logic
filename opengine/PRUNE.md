# Delete what the cut does not support

`python3 prune.py`

Biggest win: if the map does not match this terrain, drop it.

```
piles terrain    keep school-plus, pile-count, two-reads, named-G
                 drop I, leak, rate, softmax, intern, ...
thinking         rebooks I as primitive
self-cut         keep named-G, I
                 drop pile maps
no-self terrain  keep named-G
                 drop I
```

“I” is not a substrate. Thinking compresses the seeing and posts that compress as a letter `self`. Patterns do that. Primitive-output (error 1) applied to the poster.

`named-G` never drops here because every Cut has a G. That map is the method’s `A=A`. Stamp it or it will survive every prune.

Unravel: prune first. Rebook I only if `self` is still a written letter. Do not life-support an unsupported map.
