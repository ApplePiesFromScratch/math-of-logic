# Break log

`python3 break.py`

No scope claim. What printed.

---

B1 Born of `(9,6)` is **117**. The glyph `Born` will eat any pair. 117 looks like a measurement.

B2 η of `(9,6)` with itself is **45**. Same pair, other bilinear. 9²−6².

B3 rate(η(t,t), η(x,x)) = **−1**. Two metric reads, one quotient. A signed 1.

B4 lei then complex on `(1,1)`: `(1,2)` then **(−3,4)**.  
B5 complex then lei: `(0,2)` then **(0,0)**.  
Order of square-rules is a G. One order dies, the other grows.

B6 isolate seed = value `(3,3)` squared: `(9,18)` rate still **6**. Gauge again. Seed=value is legal on Q.

B7 two deads: **THETA**. Still the hole.

B8 Leibniz on two `(1/2,1/2)`: value `1/4`, channel `1/2`.  
Independence product only keeps `1/4`. The extra `1/2` is the meet they dropped.

B9 value slots of product and of lei-with-seed-1 agree (`1/4`). Channel is where they split.

B10 wrap 1: **everything 0**. A combinator that deletes all letters.

B11 swap slots of `(9,6)` then rate vs `(3,1)`: **9** not 6. Projection order is a G.

B12 D twice: `(9,6)→(6,0)→(0,0)`. Dead. D is not involutive.

B13 tensor of two isolations of 3 and 1 factors.  
B14 tensor of x with xx also factors (`27·6=18·9`). Product-shaped tensors stay product-shaped. Bell was the one that did not.

B16 Born(i*i)=Born(−1,0)=**1**.  
B17 Born(lei(1,1)²)=Born(1,2)=**5**. Dual square is not on the circle.

B18 born(sum) **26**, sum of borns **10+4=14**. Add-then-born ≠ born-then-add on Readings too.

B19 λ=−1 on listed `{-1,0,1}`: vac S=0, bump S=1, wall `(1,1,1,1)` S=**−4**. Vacuum is no longer the min. Listed V still closes. The “vacuum” name was the min of λ=+1.

B20 empty V: 0 pairs, **closes**. Vacuous close. Same hole as `all([])`.

B21 rate of leftover slots 2/1=**2**. A leftover can be fed to rate. Another official-looking numeral.

B22 xorlei on Fractions: **TypeError**. Combinator `^` is not a map of Q. Hidden partiality across hosts.

B24 η(null,null)/η(t,t)=**0**. Null interval over time interval.

B25 three different G wrote **1**. Same glyph, three mints.

---

What moved in the fog

- Square-rule order can kill the pair (B5) or enlarge it (B4).
- Born and η and rate will score any two letters. The noun does not check the mint.
- λ sign flips which config is min. “Vacuum” was a min, not empty space.
- wrap 1 and empty V both “close.” Close is cheap when V is dead.
- Gauge still holds if seed=value (B6). That does not make seed=value a unit law.
- xor is not defined on Fraction. Hosts do not share all combinators.
- 1 is the most reused leftover glyph in the folder.
