# Where to dive next

Paid enough on slots, leftover, listed field, journal, piles, contradiction-split.
The hole is: **compose those G at the size AI actually runs, still listed.**

---

## 1. Token machine (highest leverage)

List a real tokenizer V (byte or a 100-letter slice).
Decode G: logits → softmax → sample / argmax.
θ: OOV, empty ctx, overflow.
Mint: prompt + weights-hash + temperature.
Falsify: a letter leaves V and the decode still posts without θ.

This is isolate + read + project on a vocab you can print.

## 2. Autograd = Readings on a listed graph

One file: isolate, lei mix, chain, no float.
A tiny MLP on Q or Z/n.
Loss leftover named.
Compare float backprop (IEEE patch) vs Q-kernel on the same net.

## 3. Train vs test as two reads in the journal

Post every epoch: G, V, mint, train-L, test-L, leftover.
Refuse a claim that prints one number.

## 4. Attention as a rate table

Q,K listed. Score = read. Softmax project. Dead key θ.
Forge other score combinators (max, xor-shaped) on a 4-key table.
See which close.

## 5. Types and LLM code as two mints

Generate a string. Compiler θ. Runner θ.
Journal: mint_gen vs mint_run. Val-eq forbidden.

## 6. Alignment as lock-G

Like `not_einstein_vac`: two reads (helpful vs true) not locked.
A filter that forces them equal is an extra G. Stamp it.

## 7. Do not do next

- Bigger SCIENCE_MAP tables
- Another analogy carrier (DNA/econ style)
- Completing Hilbert so softmax is “exact”
- Calling 15/15 in `ml.py` a foundation model

---

## Why AI now

The same errors scale: float host, argmax as truth, one loss number, mint deleted across seeds, leftover sent to “scale.”
The journal already refuses the posts those papers need.
Token V is listed. That is the rare place the industry already lists V and then pretends the rest is R.
